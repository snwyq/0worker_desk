# 0worker_desk 2.0 升级重构计划书：专业级 AI 内容分发工作站

## 一、 产品定位与重塑 (Product Repositioning)

摆脱“实习生练手项目”的廉价感，将产品重新定义为：**面向专业内容操盘手与创作者的「AI 内容分发工作站」 (AI Content Distribution Terminal)**。

*   **设计隐喻**：如同开发者的 IDE (如 Cursor/VS Code) 或交易员的彭博终端，提供极简、高效、高信息密度的专业操作环境。
*   **核心痛点解决**：从“一文多发”升级为“**一源多端，因地制宜**”。系统能根据不同平台调性、不同账号人设（Angle/Persona），基于一个基础内容源（Source Content）自动繁衍并分发定制化内容。

---

## 二、 全新 Layout Structure（整体架构定义）

摒弃简单的“侧边栏+单页面”结构，采用**类 IDE 的多面板工作流**架构：

1. **Top Title Bar（顶部标题栏）**：
   - 沉浸式的自定义标题栏。
   - 包含：全局搜索框（Cmd/Ctrl+K 快速定位）、当前工作区名称、全局通知铃铛、用户/引擎状态指示。
2. **Left Activity Bar（最左侧活动栏 / 一级导航）**：
   - 极窄设计，仅显示高对比度的极简 Icon，无文字（悬浮显示 Tooltip）。
   - 包含核心模块：📊 **工作台**、📝 **内容裂变站**、👥 **账号矩阵**、🚀 **分发与调度**。
   - 底部固定：⚙️ **设置**、❓ **帮助**。
3. **Secondary Explorer Bar（次级资源管理器 / 左侧面板）**：
   - 根据最左侧选中的顶级模块动态切换内容。
   - 采用树状结构或“列表+过滤器”结构，用于精确导航，支持折叠。
4. **Main Editor Area（主工作区）**：
   - **Top Menu Tabs（顶部多标签页）**：支持同时打开多个内容页、账号详情页或日志页进行切换对比。
   - **Main Canvas（主画布）**：承载具体的操作表单、富文本/Markdown编辑器、左右分栏的 AI 对比视图或数据大表。
5. **Bottom Terminal / Panel（底部终端面板）**：
   - 默认折叠，按需拉起（类似 IDE 底部面板）。
   - 包含子 Tab：`运行日志 (Output)`、`分发队列 (Queue)`、`系统报警 (Alerts)`。
   - 实时滚动显示 API 调用轨迹、指纹浏览器运行状态。
6. **Status Bar（底部状态栏）**：
   - 极细的底部信息条。显示：本地/Electron Bridge 连通状态、网络延迟、AI API 消耗额度等。

---

## 三、 核心功能模块重组与层级细化

#### 1. 📝 内容裂变站 (Content Hub) - *核心模块*
**功能定位**：实现“1 Source -> AI -> N Platforms/Angles”的核心工作台。
* **Explorer Bar**：文件夹树（Inbox、草稿、已发布）、内容标签过滤、母核内容列表。
* **Top Tabs**：支持打开多个内容任务（如 `Source: 周报总结`）。
* **Main Canvas (分栏对比设计)**：
  - **左半屏 (Source Editor)**：源素材输入区（支持 Markdown / 长文本）。
  - **右半屏 (AI Transformation Console)**：
    - **目标选择**：勾选目标平台及具体账号。
    - **Angle 设定**：针对选中账号，调整专属 AI Prompt（语气、人设）。
    - **生成与比对视图**：直接预览 N 个平台/账号的不同生成版本，支持同屏独立微调。
  - **底部 Action**：确认无误后，一键 `Dispatch (推入发布队列)`。

#### 2. 👥 账号矩阵管理 (Accounts Matrix)
**功能定位**：专业管理跨平台多账号资产及其专属人设。
* **Explorer Bar**：按平台分类（Twitter, Weibo, 小红书等），按状态过滤（在线、Cookie过期等）。
* **Top Tabs**：`全局大盘`、`账号组设置`、`账号详情`。
* **Main Canvas**：
  - **列表视图 (DataGrid)**：显示头像、昵称、所属平台、Cookie有效期、最近活跃时间。
  - **操作区**：支持批量检查状态、打标签。
  - **详情页**：编辑账号专属的 **System Prompt/Persona (人设提示词)**。

#### 3. 🚀 调度与执行大盘 (Distribution & Runs)
**功能定位**：任务生命周期闭环。
* **Explorer Bar**：`活动队列 (Active)`、`执行历史 (Runs)`、`定时计划 (Cron Jobs)`。
* **Top Tabs**：`大盘视图`、`任务 #ID 详情`。
* **Main Canvas**：
  - **队列视图 (Kanban/List)**：展示文章发往各平台的状态节点（排队 -> 初始化 -> 上传 -> 遇验证码 -> 成功）。
  - **异常接管**：任务报警，点击切入指纹浏览器实况画面人工处理（如滑动验证码）。

#### 4. 📊 运营大盘概览 (Dashboard)
**功能定位**：登入首屏的数据总结与快捷入口。
* **Explorer Bar**：最近操作列表或快捷动作 (Quick Actions)。
* **Main Canvas**：
  - **核心指标**：今日裂变内容数、自动化成功率、活跃账号数。
  - **贡献热力图**：Github 风格的 30 天分发活跃度。
  - **报警聚合**：集中高亮 Cookie 过期账号或异常任务。

#### 5. ⚙️ 系统与引擎枢纽 (Settings & Help)
* **Explorer Bar**：工作区与主题、网络与代理、AI 引擎配置、帮助文档。
* **Main Canvas**：卡片化分组表单，密码/Key 暗文遮挡，提供 **API 连通性测速按钮**。

---

## 四、 UX/UI 专业化风格要求 (Codex / IDE 美学)

1. **色彩与边框 (Dark Mode 质感)**：
   - 彻底摒弃浅色护眼背景，采用深邃的深灰背景（如 `#1E1E1E` 或 `#0F111A`）。
   - 面板分割使用极细线条（`1px solid rgba(255,255,255,0.08)`），弃用粗糙边框或大面积阴影。
2. **字体排版 (Typography)**：
   - 常规 UI Text：`Inter`, `Roboto` 或 `SF Pro`。
   - 代码/账号ID/日志：**必须** 采用等宽编程字体（如 `JetBrains Mono` 或 `Fira Code`）。
3. **空间利用与表单设计**：
   - 紧凑的垂直对齐表单（Label 在 Input 上方），统一组件高度。
   - 取消大圆角，使用更锋利的 `4px` 或 `6px` 圆角（border-radius）。
4. **状态反馈机制**：
   - 绝不使用原生 `alert()`。
   - 采用高对比度的极简点缀徽章（如绿色圆点代表在线，黄点闪烁代表处理中）。

---

## 五、 给接手 AI 的执行建议

当您（其他 AI 助手）读取到这份文件时，请按以下顺序执行代码重构：

1. **基建替换 (Phase 1)**: 清理现有的 `styles.css`，引入 TailwindCSS 配置暗黑模式。建立 App.tsx 的多面板 (活动栏/左面板/主区/底部终端) 路由与组件骨架。
2. **数据流重构**: 将原来的 `view` 切换状态改为支持多 Tabs 渲染的状态管理 (可使用 Zustand 或简单的 Context)。
3. **核心突破 (Phase 2)**: 优先重构 **Content Hub (内容裂变站)**，实现 1 对 N 的分栏对比 UI。
4. **组件替换**: 所有的 Input, Button, Table 全部重写，去掉大圆角和廉价配色，替换为深色系极客风格。
