const { app, BrowserWindow } = require('electron'); app.whenReady().then(async () => { const win = new BrowserWindow({ show: false, width: 1080, height: 6000, enableLargerThanScreen: true }); await win.loadURL('data:text/html,<html><body style=\'height:6000px;background:red;\'>test</body></html>'); const bounds = win.getBounds(); console.log('Actual bounds:', bounds); app.quit(); });
